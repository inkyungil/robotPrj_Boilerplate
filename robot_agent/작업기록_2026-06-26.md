# 작업 기록 — 2026-06-26

## 목표
작은 실내 맵에서 **약 20cm 폭의 틈도 인식·통과**할 수 있도록 SLAM/내비게이션 설정 튜닝.

## 핵심 발견
- 실제 주행 로봇(pinky)이 쓰는 설정은 `/home/robotPrj/robot_agent/config/`가 **아니라**
  `/home/pinky/pinky_pro/src/pinky_pro/pinky_navigation/params/` 임.
- "맵이 벽으로 막힌 것처럼 보이던 원인" = ① 저장된 옛날 맵(resolution 0.05)을 불러온 것
  ② `inflation_radius 0.15`가 20cm 틈 양쪽 벽을 각각 15cm씩 부풀려 가운데서 겹침.
- pinky는 footprint 12cm 사각형(`[[0.06,...]]`)의 작은 로봇 (robot_radius는 주석 처리됨).

## 변경 내용 (실제 적용 = pinky_pro, src + install 양쪽)
경로: `/home/pinky/pinky_pro/.../pinky_navigation/params/`

| 항목 | 파일 | 이전 → 변경 |
|---|---|---|
| 맵 해상도 | mapper_params.yaml | 0.05 → **0.02** |
| costmap 해상도 (local/global) | nav2_params.yaml | 0.05 → **0.02** |
| inflation_radius (local/global) | nav2_params.yaml | 0.15 → **0.08** |
| cost_scaling_factor (local/global) | nav2_params.yaml | 3.0 → **10.0** |

- footprint(12cm)는 실제 크기라 유지.
- 효과: inflation 0.08 + 빠른 falloff(10.0) → 20cm 틈 가운데 약 4cm 통로 확보, 0.02 격자로 틈이 10칸으로 또렷이 표현됨.

## 백업 (오늘 날짜)
- `mapper_params_20260626.yaml`, `nav2_params_20260626.yaml`
  → pinky_pro 의 src·install params 폴더 양쪽에 원본 백업 저장됨.

## 되돌린 것
- `/home/robotPrj/robot_agent/config/{slam_params,nav2_params}.yaml`
  : 잘못 수정했던 별개 워크스페이스(실주행과 무관) → `git checkout`으로 원상복구.

## 미완료 / 보류
- **저장된 옛날 맵 파일 삭제**: 사용자 요청 있었으나 방식(완전삭제 vs 백업) 미확정으로 **보류**.
  대상: src(`asd`,`my_map`,`pinklab`) / install(`my_map`,`pinklab`) 의 .pgm/.png/.yaml
  (로고 `pinklab_logo.png`는 제외)

## 다음 할 일 — 맵 다시 그리기 (필수)
> 해상도 변경은 새 맵을 만들 때만 적용됨. 기존 0.05 맵을 0.02로 바꿀 수 없음.
1. 현재 navigation 종료
2. `map_building.launch.xml` 실행 (SLAM, 이제 0.02)
3. 로봇 주행하며 매핑 — 20cm 틈도 또렷이 찍힘
4. map_saver로 **새 이름**으로 저장
5. 새 맵으로 navigation 실행
- install 사본까지 갱신해 두었으므로 colcon 재빌드 없이 런치만 재실행하면 됨.
